<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.pearadmin.generator.mapper.GeneratorMapper">

    <select id="getTables" resultType="com.pearadmin.generator.domain.TableInfo">
        select table_name tableName, table_comment tableDescribe, table_rows dataRows, create_time createTime, update_time updateTime
        from information_schema.tables
        where table_schema = (select database())
        <if test="tableName != null and tableName != ''">
            and table_name like concat(concat('%',#{tableName,jdbcType=VARCHAR}),'%')
        </if>
        <if test="tableDescribe != null and tableDescribe != ''">
            and table_comment like concat(concat('%',#{tableDescribe,jdbcType=VARCHAR}),'%')
        </if>
    </select>

    <select id="getFields" resultType="com.pearadmin.generator.domain.FieldInfo">
        select column_name fieldName, column_comment fieldComment, data_type dataType
        from information_schema.columns
        where table_schema = (select database())
        and table_name = #{tableName}
        order by ordinal_position
    </select>

</mapper>